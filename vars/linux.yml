---
ssh:
  id_rsa:
    prvt: "{{ lookup('community.general.onepassword', 'linux-ssh-key', field='id_rsa', vault=op_vault) }}"
    pub: "{{ lookup('community.general.onepassword', 'linux-ssh-key', field='id_rsa.pub', vault=op_vault) }}"
